import type { NextPage } from "next";
import { Container } from "@mui/material";

const Home: NextPage = () => {
	return (
		<Container maxWidth="lg">
			<h1>hello</h1>
		</Container>
	);
};

export default Home;
